const horoscopes = {
    'Овен': 'Энергия на пике — завершайте проекты и избегайте споров. Направьте импульс на цели! 🔥 [web:5]',
    'Телец': 'Усилия на работе оценят. Создайте уют и наслаждайтесь стабильностью дома. 🏠 [web:8]',
    'Близнецы': 'Необычные идеи принесут успех. Будьте смелыми в общении и планах. 💡 [web:10]',
    'Рак': 'Сосредоточьтесь на семье и близких. Решатся старые вопросы в отношениях. ❤️ [web:10]',
    'Лев': 'Обаяние поможет в делах. Проявите лидерство и внимание к деталям. ✨ [web:5]',
    'Дева': 'Внимание к мелочам приведет к успеху. Завершайте задачи аккуратно. 📋 [web:12]',
    'Весы': 'Соблюдайте гармонию и спокойствие. Избегайте конфликтов, ищите компромисс. ⚖️ [web:12]',
    'Скорпион': 'Интуиция в фокусе — проявите твердость и чуткость к близким. 🌟 [web:12]',
    'Стрелец': 'Оптимизм и энергия откроют двери. Ищите поддержку у влиятельных людей. 🚀 [web:12]',
    'Козерог': 'Будьте опорой для близких. Терпение принесет плоды в работе. 🏔️ [web:19]',
    'Водолей': 'Действия важнее слов. Проявите понимание к партнеру и планам. ⚡ [web:12]',
    'Рыбы': 'День размышлений и внутреннего баланса. Доверяйте интуиции. 🌊 [web:12]'
};

// Функция getZodiacSign и getHoroscope остаются без изменений
function getZodiacSign(date) {
    const day = date.getDate();
    const month = date.getMonth() + 1;
    
    if ((month == 3 && day >= 21) || (month == 4 && day <= 19)) return 'Овен';
    if ((month == 4 && day >= 20) || (month == 5 && day <= 20)) return 'Телец';
    if ((month == 5 && day >= 21) || (month == 6 && day <= 21)) return 'Близнецы';
    if ((month == 6 && day >= 22) || (month == 7 && day <= 22)) return 'Рак';
    if ((month == 7 && day >= 23) || (month == 8 && day <= 22)) return 'Лев';
    if ((month == 8 && day >= 23) || (month == 9 && day <= 22)) return 'Дева';
    if ((month == 9 && day >= 23) || (month == 10 && day <= 23)) return 'Весы';
    if ((month == 10 && day >= 24) || (month == 11 && day <= 22)) return 'Скорпион';
    if ((month == 11 && day >= 23) || (month == 12 && day <= 21)) return 'Стрелец';
    if ((month == 12 && day >= 22) || (month == 1 && day <= 20)) return 'Козерог';
    if ((month == 1 && day >= 21) || (month == 2 && day <= 19)) return 'Водолей';
    return 'Рыбы';
}

function getHoroscope() {
    const birthdateInput = document.getElementById('birthdate');
    const result = document.getElementById('result');
    const zodiac = document.getElementById('zodiac');
    const horoscope = document.getElementById('horoscope');
    
    if (!birthdateInput.value) {
        alert('Выберите дату рождения!');
        return;
    }
    
    const birthdate = new Date(birthdateInput.value);
    const sign = getZodiacSign(birthdate);
    
    zodiac.textContent = sign;
    horoscope.textContent = horoscopes[sign].replace(/ \[web:\d+\]/, ''); // Убираем цитаты для отображения
    result.classList.remove('hidden');
}
